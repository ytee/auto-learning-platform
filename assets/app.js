(() => {
let manifest=null,C=null,currentTopic=null,state={done:{},ratings:{},bookmarks:{}};
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c]));
const storageKey=()=>`autoPrepState:${currentTopic}`;
function loadState(){state=JSON.parse(localStorage.getItem(storageKey())||'{"done":{},"ratings":{},"bookmarks":{}}');}
function save(){localStorage.setItem(storageKey(),JSON.stringify(state));progress();}
function progress(){if(!C)return;const n=C.questions.filter(q=>state.done[q.id]).length;$('#progressText').textContent=`${n} / ${C.questions.length} complete`;$('#progressBar').style.width=`${C.questions.length?100*n/C.questions.length:0}%`;}
function getFilters(){return{text:$('#search').value.trim().toLowerCase(),tier:$('#tierFilter').value,track:$('#trackFilter').value,status:$('#statusFilter').value};}
function match(q,f){const hay=[q.question,q.kind,q.tier,...q.tracks,...q.answer,...q.probes,...q.refs].join(' ').toLowerCase();return !(f.text&&!hay.includes(f.text))&&!(f.tier&&q.tier!==f.tier)&&!(f.track&&!q.tracks.includes(f.track))&&!(f.status==='done'&&!state.done[q.id])&&!(f.status==='open'&&state.done[q.id])&&!(f.status==='bookmarked'&&!state.bookmarks[q.id]);}
function card(q){const tags=[q.tier,q.kind,...q.tracks].map((t,i)=>`<span class="tag ${i===0?'tier':''}">${esc(t)}</span>`).join('');const rates=[1,2,3,4,5].map(n=>`<button class="rate ${state.ratings[q.id]===n?'selected':''}" data-rate="${n}">${n}</button>`).join('');return `<article class="question-card ${state.done[q.id]?'done':''}" data-id="${q.id}"><div class="question-top"><div><div class="meta">${tags}<span class="tag">${q.minutes} min</span></div><h3>${q.id}: ${esc(q.question)}</h3></div><button class="bookmark ${state.bookmarks[q.id]?'bookmarked':''}" title="Bookmark">${state.bookmarks[q.id]?'★':'☆'}</button></div><div class="actions"><button class="reveal">Reveal guidance</button><button class="complete">${state.done[q.id]?'Completed ✓':'Mark complete'}</button><div class="rating"><span>Self-rate</span>${rates}</div></div><div class="answer"><div class="answer-grid"><div><h4>Expected answer structure</h4><ul>${q.answer.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div><h4>Interviewer follow-ups</h4><ul>${q.probes.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></div><div class="refs"><strong>${esc(C.referenceLabel||'Reference map')}:</strong> ${q.refs.map(esc).join(' · ')}</div></div></article>`;}
function bind(container){container.querySelectorAll('.question-card').forEach(el=>{const id=el.dataset.id;el.querySelector('.reveal').onclick=e=>{const a=el.querySelector('.answer');a.classList.toggle('open');e.target.textContent=a.classList.contains('open')?'Hide guidance':'Reveal guidance';};el.querySelector('.complete').onclick=()=>{state.done[id]=!state.done[id];save();renderStudy();};el.querySelector('.bookmark').onclick=()=>{state.bookmarks[id]=!state.bookmarks[id];save();renderStudy();};el.querySelectorAll('.rate').forEach(b=>b.onclick=()=>{state.ratings[id]=Number(b.dataset.rate);save();renderStudy();});});}
function renderPlan(){const f=getFilters();$('#methodTitle').textContent=C.methodTitle;$('#methodSteps').innerHTML=C.methodSteps.map(x=>`<li>${esc(x)}</li>`).join('');$('#caseTitle').textContent=C.caseStudy.title;$('#caseDescription').textContent=C.caseStudy.description;$('#notice').textContent=C.notice;$('#dayNav').innerHTML=C.days.map(d=>`<button class="day-chip" data-day="${d.day}">Day ${d.day}</button>`).join('');$('#dayNav').querySelectorAll('button').forEach(b=>b.onclick=()=>document.getElementById(`day-${b.dataset.day}`).scrollIntoView({behavior:'smooth'}));$('#daySections').innerHTML=C.days.map(d=>{const qs=C.questions.filter(q=>q.day===d.day&&match(q,f));return `<section id="day-${d.day}" class="day-section"><header class="day-head"><div class="day-no">${d.day}</div><div><h2>${esc(d.title)}</h2><p>${esc(d.focus)}</p></div><div class="target">${esc(d.target)}</div></header><div class="deliverable"><strong>Daily deliverable:</strong> ${esc(d.deliverable)}</div><div class="question-list">${qs.length?qs.map(card).join(''):'<div class="empty">No questions match the filters.</div>'}</div></section>`;}).join('');bind($('#daySections'));}
function renderTracks(){const f=getFilters();$('#trackCards').innerHTML=C.tracks.map(t=>{const n=C.questions.filter(q=>q.tracks.includes(t.name)).length;return `<button class="track-card" data-track="${esc(t.name)}"><h3>${esc(t.name)}</h3><p>${esc(t.desc)}</p><p><strong>Interface:</strong> ${esc(t.iso)}</p><span class="count">${n} questions</span></button>`;}).join('');$('#trackCards').querySelectorAll('.track-card').forEach(b=>b.onclick=()=>{$('#trackFilter').value=b.dataset.track;$$('#trackCards .track-card').forEach(x=>x.classList.toggle('active',x===b));const qs=C.questions.filter(q=>q.tracks.includes(b.dataset.track)&&match(q,getFilters()));$('#trackQuestionResults').innerHTML=`<header class="section-head"><p class="eyebrow">SELECTED TRACK</p><h2>${esc(b.dataset.track)}</h2></header><div class="question-list">${qs.length?qs.map(card).join(''):'<div class="empty">No questions match the filters.</div>'}</div>`;bind($('#trackQuestionResults'));});if(f.track){const b=[...$('#trackCards').children].find(x=>x.dataset.track===f.track);if(b)b.click();}else $('#trackQuestionResults').innerHTML='<div class="empty">Select a topic track for focused revision.</div>';}
function renderTables(){$('#matrixTitle').textContent=C.matrixTitle;$('#matrixHeaders').innerHTML=C.matrixHeaders.map(x=>`<th>${esc(x)}</th>`).join('');$('#matrixBody').innerHTML=C.matrix.map(r=>`<tr>${r.map(c=>`<td>${esc(c)}</td>`).join('')}</tr>`).join('');$('#focusTitle').textContent=C.focusTitle;$('#focusHeaders').innerHTML=C.focusHeaders.map(x=>`<th>${esc(x)}</th>`).join('');$('#focusBody').innerHTML=C.focusMap.map(r=>`<tr>${r.map(c=>`<td>${esc(c)}</td>`).join('')}</tr>`).join('');$('#focusNote').textContent=C.focusNote;}
function renderQuiz(){$('#mockTitle').textContent=C.mockTitle;$('#quiz').innerHTML=C.quiz.map((x,i)=>`<article class="quiz-card" data-answer="${x.answer}"><h3>${i+1}. ${esc(x.q)}</h3><div class="quiz-options">${x.options.map((o,j)=>`<button class="quiz-option" data-index="${j}">${esc(o)}</button>`).join('')}</div><p class="quiz-why">${esc(x.why)}</p></article>`).join('');$$('.quiz-card').forEach(c=>c.querySelectorAll('.quiz-option').forEach(b=>b.onclick=()=>{const a=Number(c.dataset.answer),v=Number(b.dataset.index);c.querySelectorAll('.quiz-option').forEach((x,j)=>{x.disabled=true;if(j===a)x.classList.add('correct');});if(v!==a)b.classList.add('wrong');c.querySelector('.quiz-why').style.display='block';}));$('#boardTitle').textContent=C.boardExercise.title;$('#boardPrompt').textContent=C.boardExercise.prompt;$('#boardStructure').innerHTML=C.boardExercise.structure.map(x=>`<li>${esc(x)}</li>`).join('');$('#boardPressure').innerHTML=C.boardExercise.pressure.map(x=>`<li>${esc(x)}</li>`).join('');}
function renderStudy(){if(!C)return;renderPlan();renderTracks();renderTables();renderQuiz();progress();}
function populateFilters(){$('#trackFilter').innerHTML='<option value="">All tracks</option>'+C.tracks.map(t=>t.name).sort().map(t=>`<option>${esc(t)}</option>`).join('');$('#search').value='';$('#tierFilter').value='';$('#statusFilter').value='';}
function showView(name){$$('.tab').forEach(x=>x.classList.toggle('active',x.dataset.view===name));$$('.view').forEach(v=>v.classList.toggle('active',v.id===`view-${name}`));$('#studyToolbar').classList.toggle('hidden',name==='topics');if(name==='tracks'&&C)renderTracks();}
async function selectTopic(id,openPlan=true){const meta=manifest.topics.find(t=>t.id===id);if(!meta)return;C=await fetch(meta.content).then(r=>{if(!r.ok)throw new Error(`Cannot load ${meta.content}`);return r.json();});currentTopic=id;loadState();$('#topicSelect').value=id;$('#eyebrow').textContent=C.eyebrow;$('#topicTitle').textContent=C.title;$('#topicSubtitle').textContent=C.subtitle;populateFilters();renderStudy();location.hash=`topic=${encodeURIComponent(id)}`;if(openPlan)showView('plan');}
function renderTopicCards(){$('#topicCards').innerHTML=manifest.topics.map(t=>`<button class="topic-card" data-topic="${esc(t.id)}"><span class="topic-badge">10 DAYS</span><h3>${esc(t.title)}</h3><p class="topic-subtitle">${esc(t.subtitle)}</p><p>${esc(t.description)}</p><span class="open-label">Open preparation →</span></button>`).join('');$('#topicCards').querySelectorAll('button').forEach(b=>b.onclick=()=>selectTopic(b.dataset.topic,true));}
async function init(){manifest=await fetch('data/topics.json').then(r=>r.json());$('#topicSelect').innerHTML=manifest.topics.map(t=>`<option value="${esc(t.id)}">${esc(t.title)}</option>`).join('');renderTopicCards();const hash=new URLSearchParams(location.hash.replace(/^#/,''));const initial=hash.get('topic')||manifest.topics[0].id;await selectTopic(initial,false);showView('topics');}
['search','tierFilter','trackFilter','statusFilter'].forEach(id=>$('#'+id).addEventListener(id==='search'?'input':'change',renderStudy));$('#topicSelect').addEventListener('change',e=>selectTopic(e.target.value,true));$$('.tab').forEach(b=>b.onclick=()=>showView(b.dataset.view));
let seconds=45*60,timerId=null;function paint(){const m=Math.floor(seconds/60),s=seconds%60;$('#timer').textContent=`${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`;}$('#timerToggle').onclick=()=>{if(timerId){clearInterval(timerId);timerId=null;$('#timerToggle').textContent='Start';}else{timerId=setInterval(()=>{if(seconds){seconds--;paint();}else{clearInterval(timerId);timerId=null;$('#timerToggle').textContent='Start';}},1000);$('#timerToggle').textContent='Pause';}};$('#timerReset').onclick=()=>{if(timerId)clearInterval(timerId);timerId=null;seconds=45*60;paint();$('#timerToggle').textContent='Start';};paint();init().catch(e=>{console.error(e);document.body.insertAdjacentHTML('afterbegin',`<div class="fatal">${esc(e.message)}. Serve this directory through Netlify or a local HTTP server.</div>`);});
})();
